code3
+、-、^中使用^能更快让整数x变小
若是偶数，则直接^
若是奇数，为了更多地使用^则可分为两种情况：
      ①若[（x+1）/2]为偶数，则用+
      ②否则[（x-1）/2]为偶数，用  -
其中还有一种特殊情况：
      当整数x变小为3时，虽然此时符合①
      但用①后为+^^三步，比用②-^两步多
      因此，x变小为3时用②；
将选择的+、-、^放入一个一维数组里再输出即可
